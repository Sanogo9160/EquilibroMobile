import 'package:flutter/material.dart';


class Communautescreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Communauté'),
      ),
      body: Center(
        child: Text('Contenu de la communauté'),
      ),
    );
  }
}