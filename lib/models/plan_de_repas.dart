
class PlanDeRepas {
  /*
  final int id;
  final DateTime date;
  final List<Repas> repas;
  final Utilisateur utilisateur;

  PlanDeRepas({
    required this.id,
    required this.date,
    required this.repas,
    required this.utilisateur,
  });

  factory PlanDeRepas.fromJson(Map<String, dynamic> json) {
    return PlanDeRepas(
      id: json['id'],
      date: DateTime.parse(json['date']),
      repas: (json['repas'] as List)
          .map((repas) => Repas.fromJson(repas))
          .toList(),
      utilisateur: Utilisateur.fromJson(json['utilisateur']),
    );
  }

  Map<String, dynamic> toJson() {
    return {
      'id': id,
      'date': date.toIso8601String(),
      'repas': repas.map((repas) => repas.toJson()).toList(),
      'utilisateur': utilisateur.toJson(),
    };
  }

   */
}
